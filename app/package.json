{
  "name": "my-deployed-app",
  "version": "1.0.0",
  "description": "Simple Node.js app deployed on AWS EC2",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo 'Tests passed' && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
